Units
=====

Units are supported.
The default length unit is `millimeter` (mm), but can be changed to `meter` (m) or `centimeter` (cm).

To change units, use this command at the beginning of script:

.. code::

   # change units to meters
   model.units = "m"


The unit of angle is radian. This unit cannot be changed.
